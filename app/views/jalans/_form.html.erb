<%= form_with(model: jalan) do |form| %>
  <% if jalan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jalan.errors.count, "error") %> prohibited this jalan from being saved:</h2>

      <ul>
        <% jalan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_jalan %>
    <%= form.text_field :id_jalan %>
  </div>

  <div class="field">
    <%= form.label :nama_jalan %>
    <%= form.text_field :nama_jalan %>
  </div>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
