<%= form_with(model: rute) do |form| %>
  <% if rute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rute.errors.count, "error") %> prohibited this rute from being saved:</h2>

      <ul>
        <% rute.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_rute %>
    <%= form.text_field :id_rute %>
  </div>

  <div class="field">
    <%= form.label :id_angkutan %>
    <%= form.text_field :id_angkutan %>
  </div>

  <div class="field">
    <%= form.label :id_jalan %>
    <%= form.text_field :id_jalan %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
